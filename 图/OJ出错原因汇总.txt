1.在做Kruskal算法的时候，输入的边数是n*(n-1)/2 而不是 n*(n-)
2.dfs解油田问题是不需要使用并查集的。
	memset的位置要写对，要写在调用总的Traval函数内
	调用Traval在循环内部，因为多组输入
	输入的是字符数组，用char类型！！！
	标记数组用bool类型！！！！
3.Prim算法的当前最近结点的命名位置要搞清楚。
4.for循环要写清楚
5.输入图的时候，0要改成inf！！！！
6.sort(e.begin(), e.end(), [](edge a, edge b)->bool{return a.w<b.w;});！！！！！排序
7.我的理解：广度优先搜索不在乎路径是怎样的，只在乎起点在哪里，终点在哪里，然后有一个持续记录的量，例如时间。end
  我们用结构体来存储一个图结构，同时用一个结构体来存储每一个点对应的那个坐标与统计量。
  bfs即广度优先搜素需要同queue<>配合使用
  
8.putchar()不会自动换行; puts()会自动换行
9.vis[]数组一定一定一定要初始化！！！！！！！！！！！！！！！！！
10.vexnum不要忘记赋值啊啊啊啊啊